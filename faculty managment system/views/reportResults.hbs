<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Report Results - Faculty Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            background: linear-gradient(to bottom, whitesmoke, skyblue);
        }

        h3 {
            font-weight: bold;
            color: #007bff;
            margin-bottom: 1rem;
        }

        .table {
            background-color: #fff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .table thead th {
            background-color: #28a745;
            color: #fff;
            text-align: center;
        }

        .table tbody td {
            text-align: center;
            vertical-align: middle;
        }

        footer {
            background-color: #343a40;
            color: #fff;
            padding: 1rem 0;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    {{> navbar}}

    <div class="container" style="height: 48rem;">
        <h1 class="text-center" style="font-weight: bold; color: #28a745; margin-bottom: 1.5rem;">Generated Report</h1>

        {{#if isAttendance}}
            <h3>Attendance Report</h3>
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Attendance ID</th>
                        <th>Faculty ID</th>
                        <th>Date</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each results}}
                        <tr>
                            <td>{{attendance_id}}</td>
                            <td>{{faculty_id}}</td>
                            <td>{{date}}</td>
                            <td>{{status}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        {{else}}
            <h3>Salary Report</h3>
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Faculty ID</th>
                        <th>Salary</th>
                        <th>Hire Date</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each results}}
                        <tr>
                            <td>{{faculty_id}}</td>
                            <td>{{salary}}</td>
                            <td>{{hire_date}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        {{/if}}
    </div>

    <footer>
        <p class="text-center mb-0">&copy; 2024 Faculty Management System. All rights reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
