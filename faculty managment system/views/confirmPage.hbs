<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Deletion - Faculty Management System</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, whitesmoke, skyblue);
            font-family: 'Arial', sans-serif;
        }
    .container {
        max-width: 700px;
        margin-top: 2rem; 
        background: white;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    }   

        h2 {
            font-weight: bold;
            color: #d32f2f;
        }
        .alert-warning {
            background-color: #fff3cd;
            color: #856404;
            font-weight: bold;
            border-radius: 10px;
        }
        .table th {
            background-color: #f8f9fa;
            font-weight: bold;
            color: #555;
        }
        .btn-danger {
            background-color: #d32f2f;
            border: none;
        }
        .btn-danger:hover {
            background-color: #b71c1c;
        }
        .btn-secondary {
            background-color: #757575;
            border: none;
        }
        .btn-secondary:hover {
            background-color: #616161;
        }
        footer {
            background-color: black;
            color: white;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    {{> navbar}}
    <div class="container" style="height: 45rem;">
        <h2 class="text-center mb-4">Confirm Deletion</h2>
        <div class="alert alert-warning text-center">
            <p>Are you sure you want to delete the faculty member with the following details?</p>
        </div>
        <table class="table table-bordered text-center">
            <tr>
                <th>Faculty ID</th>
                <td>{{details.faculty_id}}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{details.first_name}} {{details.last_name}}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{details.email}}</td>
            </tr>
            <tr>
                <th>Phone Number</th>
                <td>{{details.phone_number}}</td>
            </tr>
            <tr>
                <th>Department</th>
                <td>{{details.department_id}}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{details.position}}</td>
            </tr>
        </table>
        <div class="text-center mt-4">
            <form action="/delete/{{details.faculty_id}}" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-danger">Yes, Delete</button>
            </form>
            <a href="/facultyManagement" class="btn btn-secondary">No, Cancel</a>
        </div>
    </div>
    <footer class="text-center py-3">
        <p>&copy; 2024 Faculty Management System. All rights reserved.</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
