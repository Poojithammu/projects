<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generate Reports - Faculty Management System</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            background: linear-gradient(to bottom, whitesmoke, skyblue);
        }
        .container {
            margin-bottom: 1rem;
        }
        h2 {
            font-weight: bold;
            color: #28a745;
            margin-bottom: 1.5rem;
        }
        .form-label {
            font-weight: bold;
        }
        .btn {
            border-radius: 5px;
        }
        .btn-success {
            background-color: #28a745;
            border-color: #28a745;
        }
        .btn-success:hover {
            background-color: #218838;
        }
        .btn-secondary {
            background-color: #6c757d;
            border-color: #6c757d;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
        }
        footer {
            background-color: #343a40;
            color: #fff;
            padding: 1rem 0;
            position: relative;
        }
    </style>
</head>
<body>
    {{> navbar}}
    <div class="container py-5">
        <h2 class="text-center">Generate Reports</h2>
        <form action="/reports" method="POST" class="shadow p-4 rounded bg-light">
            <div class="mb-3">
                <label for="report_type" class="form-label">Select Report Type</label>
                <select id="report_type" name="report_type" class="form-select" required>
                    <option value="" disabled selected>Choose report type</option>
                    <option value="attendance">Attendance Report</option>
                    <option value="salary">Salary Report</option>
                    <option value="performance">Performance Report</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="start_date" class="form-label">Start Date</label>
                <input type="date" id="start_date" name="start_date" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="end_date" class="form-label">End Date</label>
                <input type="date" id="end_date" name="end_date" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="faculty_id" class="form-label">Faculty ID (Optional)</label>
                <input type="text" id="faculty_id" name="faculty_id" class="form-control" placeholder="Leave blank for all faculty">
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-success">Generate Report</button>
                <a href="/facultyManagement" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
    <footer>
        <p class="text-center mb-0">&copy; 2024 Faculty Management System. All rights reserved.</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
