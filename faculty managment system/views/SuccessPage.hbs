<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Success - Faculty Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, whitesmoke, skyblue);
        }
        .container {
            margin-top: 1em;
        }
        h1 {
            font-weight: bold;
            color: #28a745;
        }
        p.lead {
            font-size: 1.25rem;
            color: #333;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            border-radius: 5px;
        }
        .btn-primary:hover {
            background-color: #0056b3;
        }
        footer {
            background-color: #343a40;
            color: #fff;
            padding: 1rem 0;
            position: relative;
        }
    </style>
</head>
<body>
    {{> navbar}}
    <div class="container text-center">
        <h1 class="mb-4">Success!</h1>
        {{#if updatePage}}
            <p class="lead">The details were updated successfully.</p>
        {{else if deletePage}}
            <p class="lead">The details were deleted successfully.</p>
        {{else}}
            <p class="lead">The details were added successfully.</p>
        {{/if}}
        <a href="/facultyManagement" class="btn btn-primary mt-4">Go Back to Faculty Management</a>
    </div>
    <footer>
        <p class="text-center mb-0">&copy; 2024 Faculty Management System. All rights reserved.</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
